{
  "name": "inventario-obras",
  "version": "1.0.0",
  "description": "Sistema de gestión de inventario para obras de construcción",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev:frontend": "cd frontend && npm start",
    "vercel-build": "cd frontend && npm install && npm run build"
  }
}
